from pathlib import Path
import re


def read(path):
    return Path(path).read_text()


def write(path, content):
    p = Path(path)
    p.parent.mkdir(parents=True, exist_ok=True)
    p.write_text(content)


def replace_once(path, old, new):
    text = read(path)
    if old not in text:
        raise SystemExit(f"missing expected text in {path}: {old[:120]}")
    write(path, text.replace(old, new, 1))


# Dedicated SOK catalog.
write('site/sok-batteries.html', '''<!doctype html>
<html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="theme-color" content="#050505">
<title>SOK LiFePO4 Batteries | Elevation UpScales</title><meta name="description" content="Shop SOK LiFePO4 battery systems through Elevation UpScales, an Authorized SOK Energy Dealer. Browse 12V and 48V battery options, purchase options, commercial supply and Hawaii freight review.">
<link rel="canonical" href="https://elevationupscales.com/sok-batteries"><link rel="icon" href="/assets/favicon.png" type="image/png"><link rel="stylesheet" href="/styles.css?v=3.0.3"><link rel="stylesheet" href="/site-shell.css?v=3.11.30"><link rel="stylesheet" href="/sok-commercialization.css?v=4.6.3"><script defer src="/site-shell.js?v=4.5.2"></script><script defer src="/sok-batteries.js?v=4.6.3"></script></head>
<body class="store-page sok-catalog-page"><a class="skip-link" href="#sok-products">Skip to SOK batteries</a>
<header class="eus-header"><div class="container eus-header__inner"><a class="eus-brand" href="/" aria-label="Elevation UpScales home"><img src="/assets/logo-mark.webp" width="128" height="80" alt="Elevation UpScales"><span class="eus-wordmark"><strong>ELEVATION</strong><span class="eus-wordmark__sub">UPSCALES</span><small class="eus-wordmark__tagline">Elevate the Experience</small></span></a><a class="button button-outline" href="/lithium-batteries">Lithium Batteries</a></div></header>
<main><section class="sok-hero"><div class="container sok-hero__grid"><div><p class="eyebrow">AUTHORIZED SOK ENERGY DEALER</p><h1>SOK LiFePO4 Battery Systems</h1><p>12V mobile power and 48V rack storage for RV, solar, off-grid, backup and commercial applications. Choose the battery first; Elevation handles the appropriate purchase and freight path.</p><div class="sok-actions"><a class="button button-primary" href="#sok-products">Browse SOK Batteries</a><a class="button button-outline" href="/solar-project?source=sok-batteries">Build a Solar System</a></div><p class="sok-route-note">Hawaii orders remain product-aware. Products without a qualified Hawaii route stay available to browse and begin through Freight Review Required.</p></div><figure><img src="/assets/brands/sok/sk48v100n/system-cabinet.png" alt="SOK rack battery system shown as a scalable storage example" loading="eager" decoding="async"><figcaption>System context only. Cabinets and batteries are quoted separately unless a listing explicitly says otherwise.</figcaption></figure></div></section>
<section class="sok-shop" id="sok-products"><div class="container"><div class="sok-section-head"><div><p class="eyebrow">SOK BATTERIES</p><h2>Choose your battery platform.</h2><p>Catalog visibility is separate from checkout readiness. Current purchasing options are shown on each product.</p></div><div class="sok-filter" role="group" aria-label="Filter SOK batteries"><button type="button" class="is-active" data-sok-filter="all">All</button><button type="button" data-sok-filter="12v">12V</button><button type="button" data-sok-filter="48v">48V / Rack</button></div></div><p class="sok-status" data-sok-status role="status">Loading current SOK purchase options…</p><div class="sok-grid" data-sok-grid></div><noscript><p>Initial SOK models: SK12V100PC and SK48V100N. Enable JavaScript to load current purchase modes.</p></noscript></div></section>
<section class="sok-paths"><div class="container sok-paths__grid"><article><p class="eyebrow">PURCHASE OPTIONS</p><h2>Start the order you need.</h2><p>Consumer orders, pre-purchase and backorder requests begin with the exact product and destination attached.</p><a class="button button-outline" href="/sok-order.html?sku=SK12V100PC&intent=purchase_options">See Purchase Options</a></article><article><p class="eyebrow">COMMERCIAL SUPPLY</p><h2>Volume and recurring demand.</h2><p>Installers, contractors, resellers and commercial buyers can request product-aware pricing without using ordinary consumer checkout.</p><a class="button button-outline" href="/sok-order.html?sku=SK48V100N&intent=commercial">Request Commercial Pricing</a></article><article><p class="eyebrow">HAWAII</p><h2>Freight review stays behind the order.</h2><p>Standard Hawaii requests are currently limited to 1–3 batteries. Quantity 4+ routes to commercial freight review.</p><a class="button button-outline" href="/sok-order.html?sku=SK48V100N&intent=hawaii&state=HI">Check Hawaii Availability</a></article></div></section></main>
<footer class="site-footer"><div class="container"><p>Elevation UpScales, Inc. · Authorized SOK Energy Dealer · <a href="/lithium-batteries">Lithium Batteries</a></p></div></footer></body></html>''')

write('site/sok-commercialization.css', '''.sok-catalog-page{background:#060606;color:#f5f0e6}.sok-hero{padding:clamp(3rem,7vw,6rem) 0;background:radial-gradient(circle at 80% 20%,rgba(230,198,106,.12),transparent 35%),#070707}.sok-hero__grid{display:grid;grid-template-columns:minmax(0,1fr) minmax(320px,.8fr);gap:clamp(2rem,5vw,5rem);align-items:center}.sok-hero h1{font-size:clamp(2.6rem,7vw,5.8rem);line-height:.95;max-width:11ch;margin:.5rem 0 1.2rem}.sok-hero p{max-width:64ch;line-height:1.65;color:#d8d0c1}.sok-hero figure{margin:0;border:1px solid rgba(230,198,106,.25);border-radius:24px;overflow:hidden;background:#fff}.sok-hero figure img{display:block;width:100%;aspect-ratio:4/3;object-fit:contain}.sok-hero figcaption{padding:.75rem 1rem;background:#101010;color:#c9c0b2;font-size:.85rem}.sok-actions,.sok-card__actions,.sok-merch-actions{display:flex;flex-wrap:wrap;gap:.75rem;margin-top:1.2rem}.sok-route-note{font-size:.92rem}.sok-shop,.sok-paths{padding:clamp(3rem,6vw,5rem) 0}.sok-section-head{display:flex;justify-content:space-between;gap:2rem;align-items:end}.sok-section-head h2,.sok-paths h2{margin:.25rem 0}.sok-filter{display:flex;gap:.5rem;flex-wrap:wrap}.sok-filter button{border:1px solid rgba(255,255,255,.2);background:#111;color:#eee;border-radius:999px;padding:.65rem 1rem;cursor:pointer}.sok-filter button.is-active{border-color:#e6c66a;color:#e6c66a}.sok-status{color:#c9c0b2}.sok-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:1.25rem}.sok-card{border:1px solid rgba(255,255,255,.13);border-radius:22px;overflow:hidden;background:#0d0d0d;display:grid;grid-template-columns:minmax(220px,.8fr) minmax(0,1fr)}.sok-card__media{background:#fff;display:grid;place-items:center}.sok-card__media img{width:100%;height:100%;min-height:330px;object-fit:contain}.sok-card__body{padding:1.4rem;min-width:0}.sok-card h3{font-size:clamp(1.4rem,3vw,2rem);line-height:1.05;margin:.35rem 0}.sok-card__price{font-size:1.7rem;font-weight:800;margin:.8rem 0}.sok-card__state{display:inline-block;border-radius:999px;padding:.4rem .65rem;background:rgba(230,198,106,.12);color:#e6c66a;font-weight:800}.sok-card__facts{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:.55rem;margin:1rem 0}.sok-card__facts span{border:1px solid rgba(255,255,255,.1);border-radius:12px;padding:.65rem;font-size:.9rem}.sok-card__links{display:flex;gap:1rem;flex-wrap:wrap;margin-top:1rem}.sok-card__links a{color:#e6c66a}.sok-paths{background:#0b0b0b}.sok-paths__grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:1rem}.sok-paths article,.sok-merch-panel{border:1px solid rgba(255,255,255,.12);border-radius:18px;padding:1.25rem;background:#101010}.sok-merch-panel{margin-top:1.2rem}.sok-merch-panel h2{font-size:1.25rem;margin:.35rem 0}.sok-merch-facts{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:.45rem;margin:.8rem 0}.sok-merch-facts span{padding:.5rem .65rem;border-radius:10px;background:rgba(255,255,255,.05)}.sok-merch-download{display:inline-block;color:#e6c66a;margin-top:.6rem}.sok-solar-recommendation{margin:1rem 0 1.5rem;border:1px solid rgba(230,198,106,.35);border-radius:18px;padding:1.2rem;background:linear-gradient(135deg,rgba(230,198,106,.09),rgba(255,255,255,.03))}.sok-solar-recommendation h4{font-size:1.35rem;margin:.25rem 0}.sok-solar-capacity{display:flex;gap:.5rem;flex-wrap:wrap;margin:.8rem 0}.sok-solar-capacity span{border:1px solid rgba(255,255,255,.12);border-radius:999px;padding:.4rem .65rem;font-size:.88rem}@media(max-width:900px){.sok-hero__grid,.sok-card{grid-template-columns:1fr}.sok-grid,.sok-paths__grid{grid-template-columns:1fr}.sok-section-head{align-items:start;flex-direction:column}.sok-card__media img{min-height:240px}}@media(max-width:560px){.sok-card__facts,.sok-merch-facts{grid-template-columns:1fr}.sok-actions,.sok-card__actions,.sok-merch-actions{display:grid}.sok-actions .button,.sok-card__actions .button,.sok-merch-actions .button{text-align:center}}''')

write('site/sok-batteries.js', '''(()=>{"use strict";const grid=document.querySelector("[data-sok-grid]"),status=document.querySelector("[data-sok-status]");if(!grid)return;const money=new Intl.NumberFormat("en-US",{style:"currency",currency:"USD",maximumFractionDigits:0});const esc=v=>String(v??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]));let products=[],filter="all";const track=(type,value,details={})=>window.EUSIntent?.track?.(type,value,{source:"sok-batteries",...details});function facts(p){if(p.sku==="SK12V100PC")return["12.8V","100Ah","1280Wh"];return["51.2V","100Ah","5.12kWh"]}function card(p){const bucket=p.sku==="SK12V100PC"?"12v":"48v",detail=`/product?id=${encodeURIComponent(p.id)}&store=lithium`,po=p.purchaseOptionsUrl||`/sok-order.html?sku=${encodeURIComponent(p.sku)}&intent=purchase_options`,commercial=`/sok-order.html?sku=${encodeURIComponent(p.sku)}&intent=commercial`,hawaii=`/sok-order.html?sku=${encodeURIComponent(p.sku)}&intent=hawaii&state=HI`,spec=(p.downloads||[]).find(x=>String(x.url||"").startsWith("/assets/brands/sok/"));return `<article class="sok-card" data-sok-bucket="${bucket}" data-sok-sku="${esc(p.sku)}"><a class="sok-card__media" href="${detail}"><img src="${esc(p.primaryImage)}" alt="${esc(p.title)}" loading="lazy" decoding="async"></a><div class="sok-card__body"><p class="eyebrow">SOK ENERGY · ${esc(p.sku)}</p><h3>${esc(p.title)}</h3><div class="sok-card__facts">${facts(p).map(x=>`<span>${esc(x)}</span>`).join("")}</div><p class="sok-card__price">${money.format(Number(p.priceCents||0)/100)}</p><p class="sok-card__state">${esc(p.commerceLabel||"See Purchase Options")}</p><p>${esc(p.description||"")}</p><div class="sok-card__actions"><a class="button button-primary" data-sok-action="purchase-options" href="${esc(p.purchaseUrl||po)}">${esc(p.commerceCta||"See Purchase Options")}</a><a class="button button-outline" href="${detail}">View Details</a></div><div class="sok-card__links"><a data-sok-action="commercial" href="${commercial}">Commercial Pricing</a><a data-sok-action="hawaii" href="${hawaii}">${String(p.hawaiiStatus||"").toLowerCase()==="shipping_available"?"Hawaii Shipping":"Hawaii Freight Review"}</a>${spec?`<a href="${esc(spec.url)}">Official Spec Sheet</a>`:""}</div></div></article>`}function render(){const rows=products.filter(p=>filter==="all"||(filter==="12v"&&p.sku==="SK12V100PC")||(filter==="48v"&&p.sku==="SK48V100N"));grid.innerHTML=rows.map(card).join("");status.textContent=`${rows.length} SOK battery option${rows.length===1?"":"s"}`;}document.querySelectorAll("[data-sok-filter]").forEach(btn=>btn.addEventListener("click",()=>{filter=btn.dataset.sokFilter||"all";document.querySelectorAll("[data-sok-filter]").forEach(x=>x.classList.toggle("is-active",x===btn));render();}));grid.addEventListener("click",e=>{const a=e.target.closest("a[data-sok-action]");if(!a)return;const sku=a.closest("[data-sok-sku]")?.dataset.sokSku||"";const action=a.dataset.sokAction;track(action==="commercial"?"sok_commercial_request":action==="hawaii"?"sok_hawaii_request":"sok_purchase_options_click",sku);});track("sok_collection_view","sok-batteries");fetch("/api/sok/catalog",{credentials:"same-origin",headers:{Accept:"application/json"}}).then(r=>{if(!r.ok)throw new Error("Unable to load current SOK catalog");return r.json()}).then(d=>{products=(Array.isArray(d.products)?d.products:[]).filter(p=>["SK12V100PC","SK48V100N"].includes(p.sku));render();}).catch(err=>{status.textContent=err.message;grid.innerHTML='<p>Current SOK purchase options could not be loaded. Please try again.</p>';});})();''')

write('site/sok-product-merch.js', '''(()=>{"use strict";const id=String(new URLSearchParams(location.search).get("id")||"").toLowerCase();const ids=new Set(["sok-sk12v100pc","sok-sk48v100n"]);if(!ids.has(id))return;const verified={"sok-sk12v100pc":{sku:"SK12V100PC",use:"RV, van, marine, backup and smaller off-grid systems",facts:["12.8V nominal","100Ah","1280Wh","Bluetooth","100A continuous discharge","Approx. 10.24 × 6.69 × 8.27 in"],warranty:"5-year manufacturer warranty"},"sok-sk48v100n":{sku:"SK48V100N",use:"48V solar, off-grid, backup and rack-storage systems",facts:["51.2V nominal","100Ah","5.12kWh","3U rack format","Integrated heater","RS485 / CANBus"],warranty:"7-year manufacturer warranty"}};const v=verified[id],track=(type,value,details={})=>window.EUSIntent?.track?.(type,value,{source:"sok-product",...details});fetch("/api/sok/catalog",{credentials:"same-origin",headers:{Accept:"application/json"}}).then(r=>r.ok?r.json():Promise.reject(new Error("catalog"))).then(d=>{const p=(d.products||[]).find(x=>String(x.id||"").toLowerCase()===id);if(!p)return;const summary=document.querySelector(".product-summary");if(!summary||document.querySelector("[data-sok-merch-panel]"))return;const trust=document.querySelector("[data-lithium-retailer]");if(trust){trust.hidden=false;trust.innerHTML="<strong>AUTHORIZED SOK ENERGY DEALER</strong>";}const buy=document.querySelector("[data-buy-now]");if(buy){buy.textContent=p.commerceCta||"See Purchase Options";buy.href=p.purchaseUrl||p.purchaseOptionsUrl;buy.dataset.sokPrimaryAction="true";}const freight=document.querySelector("[data-lithium-freight-link]");if(freight){freight.hidden=false;freight.href=`/sok-order.html?sku=${encodeURIComponent(v.sku)}&intent=hawaii&state=HI`;freight.textContent=String(p.hawaiiStatus||"").toLowerCase()==="shipping_available"?"Shipping to Hawaii? Review Hawaii options →":"Shipping to Hawaii? Freight Review Required →";}const note=document.querySelector(".product-note");if(note)note.textContent="Current purchase mode, destination and quantity determine whether the order uses checkout or assisted processing. Freight is confirmed separately where required.";const spec=(p.downloads||[]).find(x=>String(x.url||"").startsWith("/assets/brands/sok/"));const panel=document.createElement("section");panel.className="sok-merch-panel";panel.dataset.sokMerchPanel="true";panel.innerHTML=`<p class="eyebrow">SOK PRODUCT SUPPORT</p><h2>${v.sku} purchase options</h2><p>${v.use}. Final system compatibility is verified for the actual application.</p><div class="sok-merch-facts">${v.facts.map(x=>`<span>${x}</span>`).join("")}<span>${v.warranty}</span><span>${String(p.commerceLabel||"Current availability")}</span></div>${spec?`<a class="sok-merch-download" href="${spec.url}">Official ${v.sku} Specification Sheet</a>`:""}<div class="sok-merch-actions"><a class="button button-outline" data-sok-merch="commercial" href="/sok-order.html?sku=${encodeURIComponent(v.sku)}&intent=commercial">Request Commercial Pricing</a><a class="button button-outline" data-sok-merch="hawaii" href="/sok-order.html?sku=${encodeURIComponent(v.sku)}&intent=hawaii&state=HI">Check Hawaii Availability</a></div>`;summary.append(panel);panel.addEventListener("click",e=>{const a=e.target.closest("[data-sok-merch]");if(!a)return;track(a.dataset.sokMerch==="commercial"?"sok_commercial_request":"sok_hawaii_request",v.sku);});buy?.addEventListener("click",()=>track("sok_purchase_options_click",v.sku,{purchaseMode:p.publicPurchaseMode||""}));track("sok_product_view",v.sku,{availabilityMode:p.availabilityMode||"",purchaseMode:p.publicPurchaseMode||""});}).catch(()=>{});})();''')

write('site/sok-order.html', '''<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="theme-color" content="#070707"><title>SOK Purchase Options | Elevation UpScales</title><meta name="description" content="Start a SOK battery purchase request, commercial pricing request or Hawaii freight review through Elevation UpScales."><link rel="icon" href="/assets/favicon.png" type="image/png"><link rel="stylesheet" href="/site-shell.css?v=3.11.30"><link rel="stylesheet" href="/styles.css"><link rel="stylesheet" href="/sok-commercialization.css?v=4.6.3"><script defer src="/site-shell.js?v=4.5.2"></script><script defer src="/sok-order.js?v=4.6.3"></script></head><body><a class="skip-link" href="#main">Skip to order request</a><header class="eus-header"><div class="container eus-header__inner"><a class="eus-brand" href="/" aria-label="Elevation UpScales home"><img src="/assets/logo-mark.webp" width="128" height="80" alt="Elevation UpScales"><span class="eus-wordmark"><strong>ELEVATION</strong><span class="eus-wordmark__sub">UPSCALES</span><small class="eus-wordmark__tagline">Elevate the Experience</small></span></a><a class="button button-outline" href="/sok-batteries">SOK Batteries</a></div></header><main id="main" class="section"><div class="container" style="max-width:820px"><p class="eyebrow">AUTHORIZED SOK ENERGY DEALER</p><h1>SOK Purchase Options</h1><p>Start once with the product, quantity and destination attached. Elevation records the request internally and moves it into the appropriate consumer, commercial or Hawaii workflow.</p><section class="panel" style="margin:24px 0"><h2 id="sok-order-title">Start Your Purchase Request</h2><p id="sok-order-mode">Availability and final timing will be confirmed before the order advances.</p><div class="lithium-actions" aria-label="SOK purchase options"><button class="button button-outline" type="button" data-sok-intent="product">Ask About This Product</button><button class="button button-outline" type="button" data-sok-intent="commercial">Request Commercial Pricing</button><button class="button button-outline" type="button" data-sok-intent="hawaii">Check Hawaii Availability</button></div><form id="sok-order-form" style="display:grid;gap:14px"><input id="sok-order-intent" name="intent" type="hidden" value="purchase_options"><label>SKU<input id="sok-order-sku" name="sku" readonly required></label><label>Quantity<input id="sok-order-qty" name="quantity" type="number" min="1" max="100" value="1" required></label><label>Destination state<input id="sok-order-state" name="destinationState" maxlength="2" placeholder="CO" required></label><label>ZIP / postal code<input id="sok-order-zip" name="postalCode" maxlength="16" autocomplete="postal-code"></label><label>Name<input id="sok-order-name" name="name" autocomplete="name" required></label><label>Company <span style="font-weight:400">(if applicable)</span><input id="sok-order-company" name="company" autocomplete="organization"></label><label>Email<input id="sok-order-email" name="email" type="email" autocomplete="email" required></label><label>Phone<input id="sok-order-phone" name="phone" type="tel" autocomplete="tel"></label><label>Intended use<input id="sok-order-use" name="intendedUse" placeholder="RV, solar storage, backup, resale…"></label><label>Order need<select id="sok-order-demand" name="demandType"><option value="one_time">One-time order</option><option value="recurring">Recurring / replenishment</option></select></label><label>Notes<textarea id="sok-order-notes" name="notes" rows="4" placeholder="Project details, timing or configuration notes"></textarea></label><div class="panel"><strong>Before you submit</strong><p>No payment is collected by this form. Product availability, expected timing and any destination-specific freight requirements are confirmed before payment is required. Hawaii standard requests are currently limited to 1–3 batteries; quantity 4+ routes to commercial freight review.</p></div><button class="button button-primary" type="submit">Submit Purchase Request</button><p id="sok-order-status" role="status" aria-live="polite"></p></form></section></div></main><footer class="site-footer"><div class="container"><p>Elevation UpScales, Inc. · Authorized SOK Energy Dealer</p></div></footer></body></html>''')

write('site/sok-order.js', '''(()=>{"use strict";const params=new URLSearchParams(location.search),form=document.querySelector("#sok-order-form"),status=document.querySelector("#sok-order-status"),skuEl=document.querySelector("#sok-order-sku"),qtyEl=document.querySelector("#sok-order-qty"),stateEl=document.querySelector("#sok-order-state"),modeEl=document.querySelector("#sok-order-mode"),titleEl=document.querySelector("#sok-order-title"),intentEl=document.querySelector("#sok-order-intent");const products={SK12V100PC:{id:"sok-sk12v100pc",name:"SOK SK12V100PC 12.8V 100Ah 1280Wh LiFePO4 Battery"},SK48V100N:{id:"sok-sk48v100n",name:"SOK SK48V100N 51.2V 100Ah 5120Wh Server Rack LiFePO4 Battery"}};const sku=String(params.get("sku")||"").trim().toUpperCase(),mode=String(params.get("mode")||"").trim().toLowerCase(),product=products[sku]||{id:"",name:sku||"SOK battery"};const allowedIntents=new Set(["purchase_options","product","commercial","hawaii"]);let intent=String(params.get("intent")||"purchase_options").trim().toLowerCase();if(!allowedIntents.has(intent))intent="purchase_options";skuEl.value=sku;intentEl.value=intent;if(params.get("qty"))qtyEl.value=params.get("qty");if(params.get("state"))stateEl.value=String(params.get("state")).trim().toUpperCase();const track=(type,value,details={})=>window.EUSIntent?.track?.(type,value,{source:"sok-order",sku,...details});function setIntent(next,announce=true){if(!allowedIntents.has(next))next="purchase_options";intent=next;intentEl.value=next;if(next==="hawaii"){stateEl.value="HI";titleEl.textContent="Request Hawaii Purchase Options";if(announce)status.textContent="Hawaii selected. Standard requests are 1–3 batteries; quantity 4+ routes to commercial freight review.";}else if(next==="commercial"){titleEl.textContent="Request Commercial Pricing";if(announce)status.textContent="Commercial pricing selected. Add quantity, destination, company and intended use below.";}else if(next==="product"){titleEl.textContent="Ask About This Product";if(announce)status.textContent="Product inquiry selected. Your exact SOK model stays attached to this request.";}else{titleEl.textContent="Start Your Purchase Request";}if(next==="commercial")track("sok_commercial_request",sku,{stage:"started"});else if(next==="hawaii")track("sok_hawaii_request",sku,{stage:"started"});else track("sok_purchase_options_click",sku,{stage:"opened",intent:next});}if(mode==="prepurchase")modeEl.textContent="PRE-PURCHASE — Expected fulfillment timing is an estimate and may change. Elevation confirms the current supply window before payment when required.";else if(mode==="backorder")modeEl.textContent="AVAILABLE ON BACKORDER — Elevation confirms replenishment and expected fulfillment timing before payment when required.";else modeEl.textContent="Availability and final timing will be confirmed before the order advances.";document.querySelectorAll("[data-sok-intent]").forEach(btn=>btn.addEventListener("click",()=>{setIntent(String(btn.dataset.sokIntent||"purchase_options"));form.scrollIntoView({behavior:"smooth",block:"start"});}));setIntent(intent,false);form?.addEventListener("submit",async e=>{e.preventDefault();status.textContent="Submitting purchase request…";const body={intent:intentEl.value,sku:skuEl.value,quantity:Number.parseInt(qtyEl.value||"1",10)||1,destinationState:stateEl.value.trim().toUpperCase(),postalCode:document.querySelector("#sok-order-zip").value.trim(),name:document.querySelector("#sok-order-name").value.trim(),company:document.querySelector("#sok-order-company").value.trim(),email:document.querySelector("#sok-order-email").value.trim(),phone:document.querySelector("#sok-order-phone").value.trim(),intendedUse:document.querySelector("#sok-order-use").value.trim(),demandType:document.querySelector("#sok-order-demand").value,notes:document.querySelector("#sok-order-notes").value.trim(),productUrl:product.id?`${location.origin}/product?id=${encodeURIComponent(product.id)}&store=lithium`:location.href};try{const r=await fetch("/api/sok/reservations",{method:"POST",credentials:"same-origin",headers:{"Content-Type":"application/json",Accept:"application/json"},body:JSON.stringify(body)}),d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||"Unable to start the order");status.textContent=`${d.status}. Reference ${d.reservationId}. ${d.nextStep}`;form.querySelector("button[type=submit]").disabled=true;const event=intentEl.value==="commercial"?"sok_commercial_request":intentEl.value==="hawaii"?"sok_hawaii_request":"sok_purchase_options_click";track(event,sku,{stage:"submitted",quantity:body.quantity,destination:body.destinationState});}catch(err){status.textContent=err.message;}});})();''')

# Add first-class customer-safe inquiry fields to the existing reservation record.
p = 'site/sok-availability-runtime.js'
text = read(p)
marker = '  await db.prepare("CREATE INDEX IF NOT EXISTS idx_eus_sok_reservations_created ON eus_sok_customer_reservations(created_at DESC)").run();'
if 'ADD COLUMN inquiry_intent' not in text:
    additions = '''  await addColumn(db,"ALTER TABLE eus_sok_customer_reservations ADD COLUMN inquiry_intent TEXT NOT NULL DEFAULT 'purchase_options'");\n  await addColumn(db,"ALTER TABLE eus_sok_customer_reservations ADD COLUMN company TEXT NOT NULL DEFAULT ''");\n  await addColumn(db,"ALTER TABLE eus_sok_customer_reservations ADD COLUMN postal_code TEXT NOT NULL DEFAULT ''");\n  await addColumn(db,"ALTER TABLE eus_sok_customer_reservations ADD COLUMN intended_use TEXT NOT NULL DEFAULT ''");\n  await addColumn(db,"ALTER TABLE eus_sok_customer_reservations ADD COLUMN demand_type TEXT NOT NULL DEFAULT 'one_time'");\n'''
    if marker not in text:
        raise SystemExit('reservation index marker missing')
    text = text.replace(marker, additions + marker, 1)
pattern = re.compile(r'async function createReservation\(request,env\)\{.*?\n\nasync function availabilityAdminSnapshot', re.S)
replacement = '''async function createReservation(request,env){
  if(request.method!=="POST")return json({error:"Method not allowed"},405);
  if(!sameOrigin(request))return json({error:"Cross-origin request denied"},403);
  const raw=await request.json().catch(()=>({}));
  const sku=upper(raw.sku,120);if(!SOK_PUBLIC[sku])return json({error:"SOK product not found"},404);
  const intents=new Set(["purchase_options","product","commercial","hawaii"]);
  const inquiryIntent=intents.has(clean(raw.intent,40).toLowerCase())?clean(raw.intent,40).toLowerCase():"purchase_options";
  const name=clean(raw.name,120),email=clean(raw.email,180).toLowerCase(),phone=clean(raw.phone,60),notes=clean(raw.notes,2000),destination=upper(raw.destinationState,2),quantity=Math.max(1,Math.min(100,int(raw.quantity,1)));
  const company=clean(raw.company,180),postalCode=clean(raw.postalCode,24),intendedUse=clean(raw.intendedUse,400),demandType=clean(raw.demandType,40).toLowerCase()==="recurring"?"recurring":"one_time",productUrl=clean(raw.productUrl,1000);
  if(!name||!validEmail(email))return json({error:"Name and valid email are required"},400);
  const db=await ensureSokAvailabilitySchema(env);const row=await db.prepare("SELECT * FROM eus_sok_product_ops WHERE upper(sku)=? LIMIT 1").bind(sku).first();const availability=commercePresentation(row,destination);
  const commercial=inquiryIntent==="commercial"||(destination==="HI"&&quantity>3);const id=`SOK-RES-${crypto.randomUUID()}`,stamp=now();
  const context=[notes,productUrl?`Product URL: ${productUrl}`:""] .filter(Boolean).join("; ");
  await db.prepare("INSERT INTO eus_sok_customer_reservations (id,sku,quantity,availability_mode,destination_state,customer_name,customer_email,customer_phone,customer_notes,status,commercial_quantity,created_at,updated_at,inquiry_intent,company,postal_code,intended_use,demand_type) VALUES (?,?,?,?,?,?,?,?,?,'RECEIVED',?,?,?,?,?,?,?,?)").bind(id,sku,quantity,availability.mode,destination,name,email,phone,context,commercial?1:0,stamp,stamp,inquiryIntent,company,postalCode,intendedUse,demandType).run();
  try{await db.prepare("INSERT INTO eus_sok_events (id,entity_type,entity_id,action,details_json,actor,created_at) VALUES (?,?,?,?,?,?,?)").bind(`SOK-EVT-${crypto.randomUUID()}`,"customer_reservation",id,"received",JSON.stringify({sku,quantity,destination,inquiryIntent,demandType,availabilityMode:availability.mode,commercialQuantity:commercial}),"public-reservation",stamp).run();}catch(_){}
  const status=commercial?"COMMERCIAL INQUIRY RECEIVED":inquiryIntent==="hawaii"?"HAWAII PURCHASE REQUEST RECEIVED":"RESERVATION RECEIVED";
  return json({ok:true,reservationId:id,sku,quantity,status,paymentCollected:false,nextStep:commercial?"Elevation will review the commercial configuration and contact you.":inquiryIntent==="hawaii"?"Elevation will confirm the product-specific Hawaii freight path before payment is required.":"Elevation will confirm availability, fulfillment timing and any required freight before the order advances."},201);
}

async function availabilityAdminSnapshot'''
if not pattern.search(text):
    raise SystemExit('createReservation pattern missing')
text = pattern.sub(replacement, text, count=1)
pattern2 = re.compile(r'async function availabilityAdminSnapshot\(db\)\{.*?\n\nasync function patchAvailability', re.S)
replacement2 = '''async function availabilityAdminSnapshot(db){const products=await db.prepare("SELECT sku,availability_mode,public_purchase_mode,prepurchase_enabled,backorder_enabled,expected_available_date,expected_ship_window,supplier_replenishment_confirmed,customer_timing_notice,management_approved,lifecycle_state,hazmat_document_state,hazmat_state,carrier_state,economics_state,hawaii_eligible,commercial_ready,updated_at,updated_by FROM eus_sok_product_ops WHERE upper(sku) IN ('SK12V100PC','SK48V100N') ORDER BY CASE upper(sku) WHEN 'SK12V100PC' THEN 0 ELSE 1 END").all();const reservations=await db.prepare("SELECT id,sku,quantity,availability_mode,destination_state,customer_name,customer_email,customer_phone,status,commercial_quantity,inquiry_intent,company,postal_code,intended_use,demand_type,created_at FROM eus_sok_customer_reservations ORDER BY created_at DESC LIMIT 100").all();return{products:(products.results||[]).map(r=>({...r,commerce:commercePresentation(r,"")})),reservations:reservations.results||[],carrierQualification:{primary:"H2O Logistics",backup:"Approved Freight Forwarders",origin:"4277 Schaefer Ave, Chino, CA 91710",priorityQuantities:[1,2,3],packages:Object.values(SOK_PUBLIC).map(d=>({sku:d.sku,model:d.sku,voltage:d.voltage,wh:d.energyWh,grossWeightLb:d.grossWeightLb,carton:d.carton,documents:["MSDS / SDS","UN38.3 full test report","UN38.3 test summary","transport documentation"],carrierAcceptance:"NOT YET PROVEN",economicsApproval:"NOT YET PROVEN"}))}};}

async function patchAvailability'''
if not pattern2.search(text):
    raise SystemExit('availability snapshot pattern missing')
text = pattern2.sub(replacement2, text, count=1)
text = text.replace('export const __sokAvailabilityTest={commercePresentation,SAFE_TIMING_NOTICE,PURCHASE_MODES};', 'export const __sokAvailabilityTest={commercePresentation,SAFE_TIMING_NOTICE,PURCHASE_MODES,SOK_PUBLIC,publicProduct};')
write(p, text)

# Pure Hawaii quantity classification for deterministic CI without database writes.
p = 'site/sok-operations-runtime.js'
text = read(p)
if 'export function classifySokHawaiiQuantity' not in text:
    text = text.replace('export const HAWAII_OPERATIONAL_MAX_QTY = 3;', 'export const HAWAII_OPERATIONAL_MAX_QTY = 3;\nexport function classifySokHawaiiQuantity(quantity=1){const parsed=Number.parseInt(String(quantity??1),10);const q=Math.max(1,Number.isInteger(parsed)?parsed:1);return{quantity:q,standard:q<=HAWAII_OPERATIONAL_MAX_QTY,commercial:q>HAWAII_OPERATIONAL_MAX_QTY,maxStandardQuantity:HAWAII_OPERATIONAL_MAX_QTY};}', 1)
old = 'const q=Math.max(1,integer(quantity,1));if(q>HAWAII_OPERATIONAL_MAX_QTY){'
new = 'const quantityClass=classifySokHawaiiQuantity(quantity),q=quantityClass.quantity;if(quantityClass.commercial){'
if old not in text:
    raise SystemExit('Hawaii quantity evaluation marker missing')
text = text.replace(old, new, 1)
write(p, text)

# Correct legacy SOK Hawaii flat-freight presentation client-side.
p = 'site/lithium-shop.js'
text = read(p)
old = 'const hawaiiPriceReady = !product?.sokProduct || String(product?.hawaiiStatus || "").toLowerCase() === "shipping_available";'
if old not in text:
    raise SystemExit('client Hawaii price marker missing')
text = text.replace(old, 'const hawaiiPriceReady = !product?.sokProduct;', 1)
old = '${hawaiiMode ? `<div class="hawaii-card-simple"><p><strong>Availability</strong><span>${esc(inventory)}</span></p><p><strong>Hawaii Pickup Price</strong><span data-hawaii-pickup-price>${batteryUnits > 0 && hawaiiPriceReady ? money.format(pickupPriceCents / 100) : "Freight Review Required"}</span></p><p><strong>Included Freight</strong><span>To our Honolulu warehouse / pickup location</span></p><p><strong>Address Delivery</strong><span>Additional charge · quote required</span></p></div>` : ""}'
new = '${hawaiiMode ? (product?.sokProduct ? `<div class="hawaii-card-simple"><p><strong>Hawaii Availability</strong><span>${String(product?.hawaiiStatus||"").toLowerCase()==="shipping_available"?"Shipping Available":"Freight Review Required"}</span></p><p><strong>Standard Quantity</strong><span>1–3 batteries</span></p><p><strong>Commercial Quantity</strong><span>4+ · Freight Review Required</span></p></div>` : `<div class="hawaii-card-simple"><p><strong>Availability</strong><span>${esc(inventory)}</span></p><p><strong>Hawaii Pickup Price</strong><span data-hawaii-pickup-price>${batteryUnits > 0 && hawaiiPriceReady ? money.format(pickupPriceCents / 100) : "Freight Review Required"}</span></p><p><strong>Included Freight</strong><span>To our Honolulu warehouse / pickup location</span></p><p><strong>Address Delivery</strong><span>Additional charge · quote required</span></p></div>`) : ""}'
if old not in text:
    raise SystemExit('client Hawaii block missing')
text = text.replace(old, new, 1)
write(p, text)

# Same Hawaii presentation server-side/prerendered.
p = 'site/_worker.js'
text = read(p)
old = 'const hawaiiPriceReady=!product?.sokProduct||clean(product?.hawaiiStatus,40).toLowerCase()==="shipping_available";'
if old not in text:
    raise SystemExit('worker Hawaii price marker missing')
text = text.replace(old, 'const hawaiiPriceReady=!product?.sokProduct;', 1)
old = '${hawaii?`<div class="hawaii-card-simple"><p><strong>Availability</strong><span>${htmlEsc(inventory)}</span></p><p><strong>Hawaii Pickup Price</strong><span data-hawaii-pickup-price>${batteryUnits>0&&hawaiiPriceReady?htmlEsc(money(pickupPriceCents)):"Freight Review Required"}</span></p><p><strong>Included Freight</strong><span>To our Honolulu warehouse / pickup location</span></p><p><strong>Address Delivery</strong><span>Additional charge · quote required</span></p></div>`:""}'
new = '${hawaii?(product?.sokProduct?`<div class="hawaii-card-simple"><p><strong>Hawaii Availability</strong><span>${clean(product?.hawaiiStatus,40).toLowerCase()==="shipping_available"?"Shipping Available":"Freight Review Required"}</span></p><p><strong>Standard Quantity</strong><span>1–3 batteries</span></p><p><strong>Commercial Quantity</strong><span>4+ · Freight Review Required</span></p></div>`:`<div class="hawaii-card-simple"><p><strong>Availability</strong><span>${htmlEsc(inventory)}</span></p><p><strong>Hawaii Pickup Price</strong><span data-hawaii-pickup-price>${batteryUnits>0&&hawaiiPriceReady?htmlEsc(money(pickupPriceCents)):"Freight Review Required"}</span></p><p><strong>Included Freight</strong><span>To our Honolulu warehouse / pickup location</span></p><p><strong>Address Delivery</strong><span>Additional charge · quote required</span></p></div>`):""}'
if old not in text:
    raise SystemExit('worker Hawaii block missing')
text = text.replace(old, new, 1)
write(p, text)

# Finish generic product page specifically for SOK via additive merch script.
replace_once('site/product.html', '<link rel="stylesheet" href="/product-detail.css?v=4.4.1">', '<link rel="stylesheet" href="/product-detail.css?v=4.4.1"><link rel="stylesheet" href="/sok-commercialization.css?v=4.6.3">')
replace_once('site/product.html', '<script defer src="/product-detail.js?v=4.4.1"></script>', '<script defer src="/product-detail.js?v=4.4.1"></script><script defer src="/sok-product-merch.js?v=4.6.3"></script>')

# Strong SOK discovery without a new top-level global navigation item.
p = 'site/lithium-batteries.html'
text = read(p)
old = '<div class="lithium-actions"><a class="button button-primary" href="/product?id=sok-sk48v100n&amp;store=lithium">View 48V Rack Battery</a><a class="button button-outline" href="/product?id=sok-sk12v100pc&amp;store=lithium">View 12V 100Ah Battery</a></div>'
new = '<div class="lithium-actions"><a class="button button-primary" href="/sok-batteries">Shop SOK Batteries</a><a class="button button-outline" href="/product?id=sok-sk48v100n&amp;store=lithium">View 48V Rack Battery</a><a class="button button-outline" href="/product?id=sok-sk12v100pc&amp;store=lithium">View 12V 100Ah Battery</a></div>'
if old not in text:
    raise SystemExit('lithium SOK feature actions missing')
text = text.replace(old, new, 1)
old = '<p>Eligible Hawaii batteries use <strong>$99 per actual battery</strong> to the Honolulu warehouse / freight-terminal pickup location. Delivery beyond pickup is a separate quote where available. Alaska shipping remains Freight Review Required.</p>'
new = '<p>Some currently qualified Hawaii battery listings use <strong>$99 per actual battery</strong> to the Honolulu warehouse / freight-terminal pickup location. SOK products do not inherit that flat freight assumption: they show <strong>Freight Review Required</strong> until the exact product route is qualified. Alaska shipping remains Freight Review Required.</p>'
if old not in text:
    raise SystemExit('lithium Hawaii paragraph missing')
text = text.replace(old, new, 1)
write(p, text)

# Targeted shop-dropdown discovery under Lithium/Power, not a top-level item.
p = 'site/site-shell.js'
text = read(p)
old = '["/lithium-batteries", "Lithium Battery Shop", "LiFePO4 batteries for RV, solar, van, and off-grid power"],\n      ["/hawaii-lithium-batteries", "Hawaii Power & Logistics", "Eligible battery models · Hawaii freight intake"],'
new = '["/lithium-batteries", "Lithium Battery Shop", "LiFePO4 batteries for RV, solar, van, and off-grid power"],\n      ["/sok-batteries", "SOK Battery Systems", "Authorized SOK Energy Dealer · 12V and 48V LiFePO4"],\n      ["/hawaii-lithium-batteries", "Hawaii Power & Logistics", "Eligible battery models · Hawaii freight intake"],'
if old not in text:
    raise SystemExit('site shell lithium nav marker missing')
text = text.replace(old, new, 1)
write(p, text)

# Restrained homepage merchandising: existing product grid stays authoritative.
p = 'site/index.html'
text = read(p)
old = '<div class="home-commerce__group-head"><h3>Lithium Batteries</h3><a class="button button-outline" href="/lithium-batteries">Shop All Lithium Batteries</a></div>'
new = '<div class="home-commerce__group-head"><h3>Lithium Batteries</h3><div class="hc-actions"><a class="button button-outline" href="/sok-batteries">SOK Battery Systems</a><a class="button button-outline" href="/lithium-batteries">Shop All Lithium Batteries</a></div></div>'
if old not in text:
    raise SystemExit('home lithium group marker missing')
text = text.replace(old, new, 1)
write(p, text)

# Solar Builder recommendation is additive and does not alter its engineering model.
write('site/sok-solar-builder.js', '''(()=>{"use strict";const track=(type,value,details={})=>window.EUSIntent?.track?.(type,value,{source:"solar-builder",...details});function mount(){if(document.querySelector("[data-sok-solar-recommendation]"))return;const battery=document.querySelector("#batteries")||document.querySelector('[data-builder-section="batteries"]')||document.querySelector("#builder .builder-main");if(!battery)return;const box=document.createElement("aside");box.className="sok-solar-recommendation";box.dataset.sokSolarRecommendation="true";box.innerHTML='<p class="eyebrow">SOK STORAGE OPTION</p><h4>SOK SK48V100N · 48V Rack Storage</h4><p>51.2V / 100Ah / 5.12kWh per battery. Use this as a planning reference for appropriate 48V storage architectures; final inverter, BMS, protection and system compatibility are verified during professional review.</p><div class="sok-solar-capacity"><span>1 = 5.12kWh</span><span>2 = 10.24kWh</span><span>3 = 15.36kWh</span><span>4 = 20.48kWh</span></div><div class="sok-merch-actions"><a class="button button-outline" data-sok-solar="product" href="/product?id=sok-sk48v100n&store=lithium">View SK48V100N</a><a class="button button-outline" data-sok-solar="options" href="/sok-order.html?sku=SK48V100N&intent=purchase_options">See Purchase Options</a></div>';battery.append(box);box.addEventListener("click",e=>{const a=e.target.closest("[data-sok-solar]");if(a)track("solar_builder_sok_click","SK48V100N",{action:a.dataset.sokSolar});});}if(document.readyState==="loading")document.addEventListener("DOMContentLoaded",mount);else mount();})();''')
p = 'site/solar-project.html'
text = read(p)
marker = '<script defer="" src="/solar-builder.js?v=3.11.23-solar-intake"></script>'
if marker not in text:
    raise SystemExit('solar builder script marker missing')
text = text.replace(marker, marker + '\n<link href="/sok-commercialization.css?v=4.6.3" rel="stylesheet"/><script defer="" src="/sok-solar-builder.js?v=4.6.3"></script>', 1)
write(p, text)
write('site/solar-project/index.html', text)

# Deterministic static QA; no production records are mutated.
write('deployment/qa/sok-commercialization-static.mjs', '''import fs from "node:fs";import assert from "node:assert/strict";import {__sokAvailabilityTest} from "../../site/sok-availability-runtime.js";import {classifySokHawaiiQuantity,HAWAII_OPERATIONAL_MAX_QTY} from "../../site/sok-operations-runtime.js";
const {commercePresentation,SOK_PUBLIC,publicProduct}=__sokAvailabilityTest;const base={availability_mode:"available",management_approved:1,prepurchase_enabled:1,backorder_enabled:1,supplier_replenishment_confirmed:1,expected_ship_window:"2–4 weeks",customer_timing_notice:"Timing estimate",lower48_eligible:1,hawaii_eligible:0,updated_at:"qa"};
const modes={CATALOG_ONLY:"View Product",CONTACT_TO_ORDER:"Email Us to Order",PURCHASE_OPTIONS:"See Purchase Options",DIRECT_CHECKOUT:"Buy Now",UNAVAILABLE:"Currently Unavailable"};for(const [mode,cta] of Object.entries(modes)){const out=commercePresentation({...base,public_purchase_mode:mode});assert.equal(out.cta,cta);if(mode!=="DIRECT_CHECKOUT")assert.equal(out.paymentEligible,false);}
let out=commercePresentation({...base,availability_mode:"prepurchase",public_purchase_mode:"DIRECT_CHECKOUT"});assert.equal(out.label,"Pre-Purchase");assert.equal(out.requiresTimingAcknowledgement,true);out=commercePresentation({...base,availability_mode:"backorder",public_purchase_mode:"DIRECT_CHECKOUT"});assert.equal(out.label,"Available on Backorder");
assert.equal(HAWAII_OPERATIONAL_MAX_QTY,3);assert.deepEqual(classifySokHawaiiQuantity(1),{quantity:1,standard:true,commercial:false,maxStandardQuantity:3});assert.equal(classifySokHawaiiQuantity(3).standard,true);assert.equal(classifySokHawaiiQuantity(4).commercial,true);
assert.equal(SOK_PUBLIC.SK12V100PC.mapCents,31900);assert.equal(SOK_PUBLIC.SK48V100N.mapCents,119900);for(const sku of ["SK12V100PC","SK48V100N"]){const p=publicProduct({...base,sku,public_purchase_mode:"PURCHASE_OPTIONS"});assert.ok(p.priceCents>=SOK_PUBLIC[sku].mapCents);assert.equal(p.promotion.eligible,false);assert.equal(p.promotion.couponEligible,false);assert.equal(p.promotion.pricingMode,"sok-map");const raw=JSON.stringify(p);for(const token of ["supplierCost","supplierInventory","sourceWarehouse","dropShipCost","landedCost","marginCents","primary_carrier","carrier_state","economics_state"])assert.equal(raw.includes(token),false,`public leak ${token}`);}
for(const file of ["site/sok-batteries.html","site/sok-batteries.js","site/sok-product-merch.js","site/sok-order.html","site/sok-order.js","site/sok-solar-builder.js","site/assets/brands/sok/sk12v100pc/hero.png","site/assets/brands/sok/sk12v100pc/spec-sheet.pdf","site/assets/brands/sok/sk48v100n/hero.jpg","site/assets/brands/sok/sk48v100n/spec-sheet.pdf","site/assets/brands/sok/sk48v100n/system-cabinet.png"])assert.ok(fs.existsSync(file),`missing ${file}`);
const order=fs.readFileSync("site/sok-order.html","utf8")+fs.readFileSync("site/sok-order.js","utf8");assert.equal(/mailto:casey@elevationupscales\.com/i.test(order),false);assert.match(order,/\/api\/sok\/reservations/);assert.match(order,/Request Commercial Pricing/);assert.match(order,/Check Hawaii Availability/);const client=fs.readFileSync("site/lithium-shop.js","utf8");assert.match(client,/Standard Quantity/);assert.match(client,/4\+ · Freight Review Required/);const worker=fs.readFileSync("site/_worker.js","utf8");assert.match(worker,/Standard Quantity/);assert.match(worker,/4\+ · Freight Review Required/);const solar=fs.readFileSync("site/solar-project.html","utf8");assert.match(solar,/sok-solar-builder\.js/);assert.equal(solar,fs.readFileSync("site/solar-project\/index.html","utf8"));const analytics=["sok_collection_view","sok_product_view","sok_purchase_options_click","sok_commercial_request","sok_hawaii_request","solar_builder_sok_click"];const all=["site/sok-batteries.js","site/sok-product-merch.js","site/sok-order.js","site/sok-solar-builder.js"].map(x=>fs.readFileSync(x,"utf8")).join("\n");for(const event of analytics)assert.match(all,new RegExp(event));console.log("SOK commercialization static gate: PASS");''')

write('deployment/qa/sok-commercialization-preview.sh', '''#!/usr/bin/env bash
set -euo pipefail
base="${1%/}"; test -n "$base"
routes=("/" "/store" "/rv-store" "/lithium-batteries" "/sok-batteries" "/product?id=sok-sk12v100pc&store=lithium" "/product?id=sok-sk48v100n&store=lithium" "/sok-order.html?sku=SK12V100PC&intent=purchase_options" "/sok-order.html?sku=SK48V100N&intent=commercial" "/hawaii-lithium-batteries" "/solar-project" "/checkout/" "/start-a-project" "/marketplace")
for route in "${routes[@]}"; do code=$(curl -sS -L --retry 8 --retry-all-errors --retry-delay 2 --connect-timeout 15 --max-time 45 -o /tmp/sok-route -w '%{http_code}' "${base}${route}" || true); [[ "$code" == 200 ]] || { echo "FAIL route $route = $code"; exit 1; }; done
curl -sS -L --retry 8 --retry-all-errors "${base}/sok-batteries" -o /tmp/sok-page; grep -q 'AUTHORIZED SOK ENERGY DEALER' /tmp/sok-page; grep -q 'SK12V100PC' /tmp/sok-page; grep -q 'SK48V100N' /tmp/sok-page
curl -sS -L --retry 8 --retry-all-errors "${base}/sok-order.html?sku=SK48V100N&intent=commercial" -o /tmp/sok-order; grep -q 'Request Commercial Pricing' /tmp/sok-order; ! grep -qi 'mailto:casey@elevationupscales.com' /tmp/sok-order
curl -sS -L --retry 8 --retry-all-errors "${base}/api/sok/catalog" -o /tmp/sok-catalog.json
python3 - <<'PY2'
import json
d=json.load(open('/tmp/sok-catalog.json')); rows={p['sku']:p for p in d.get('products',[])}
assert set(['SK12V100PC','SK48V100N']) <= set(rows)
assert rows['SK12V100PC']['priceCents'] >= 31900
assert rows['SK48V100N']['priceCents'] >= 119900
assert rows['SK12V100PC']['availabilityMode'] == 'available'
assert rows['SK48V100N']['availabilityMode'] == 'prepurchase'
for p in rows.values():
    promo=p.get('promotion',{}); assert promo.get('eligible') is False; assert promo.get('couponEligible') is False; assert promo.get('pricingMode') == 'sok-map'
raw=json.dumps(d).lower()
for token in ['suppliercost','supplierinventory','sourcewarehouse','dropshipcost','landedcost','margincents','primary_carrier','carrier_state','economics_state']:
    assert token not in raw, token
PY2
for asset in /assets/brands/sok/sk12v100pc/hero.png /assets/brands/sok/sk12v100pc/spec-sheet.pdf /assets/brands/sok/sk48v100n/hero.jpg /assets/brands/sok/sk48v100n/spec-sheet.pdf /assets/brands/sok/sk48v100n/system-cabinet.png /sok-product-merch.js /sok-solar-builder.js; do code=$(curl -sS -L --retry 8 --retry-all-errors -o /dev/null -w '%{http_code}' "${base}${asset}"); [[ "$code" == 200 ]] || { echo "FAIL asset $asset = $code"; exit 1; }; done
for protected in /sok-availability-runtime.js /sok-operations-runtime.js /commerce-pricing-runtime.js /catalog-admin-runtime.js /doba-csv-sync-runtime.js; do code=$(curl -sS -o /dev/null -w '%{http_code}' "${base}${protected}" || true); [[ "$code" == 404 ]] || { echo "FAIL protected $protected = $code"; exit 1; }; done
code=$(curl -sS -o /dev/null -w '%{http_code}' "${base}/api/admin/sok-availability" || true); [[ "$code" == 401 ]] || { echo "FAIL SOK admin auth = $code"; exit 1; }
curl -sS -L "${base}/solar-project" | grep -q 'sok-solar-builder.js'
curl -sS -L "${base}/lithium-shop.js" | grep -q '4+ · Freight Review Required'
echo "SOK commercialization preview smoke: PASS"
''')
Path('deployment/qa/sok-commercialization-preview.sh').chmod(0o755)

# Extend existing RC gate; preserve preview-only behavior.
p = '.github/workflows/release-candidate-gate.yml'
text = read(p)
if 'work/sok-4-6-catalog-commercialization-2026-09-04' not in text.split('workflow_dispatch:', 1)[0]:
    text = text.replace('on:\n  workflow_dispatch:', 'on:\n  push:\n    branches:\n      - work/sok-4-6-catalog-commercialization-2026-09-04\n  workflow_dispatch:', 1)
expr = lambda value: '$' + '{{ ' + value + ' }}'
text = text.replace('ref: ' + expr('inputs.candidate_ref'), 'ref: ' + expr("github.event_name == 'workflow_dispatch' && inputs.candidate_ref || github.sha"), 1)
text = text.replace('EXPECTED_PARENT: ' + expr('inputs.expected_parent_sha'), 'EXPECTED_PARENT: ' + expr("github.event_name == 'workflow_dispatch' && inputs.expected_parent_sha || ''"), 1)
old = 'expected_parent="$(printf \'%s\' "$EXPECTED_PARENT" | tr \'[:upper:]\' \'[:lower:]\' | tr -d \'[:space:]\')"\n\n          if [[ "$expected_parent" != "$current_main" ]]; then'
new = 'expected_parent="$(printf \'%s\' "$EXPECTED_PARENT" | tr \'[:upper:]\' \'[:lower:]\' | tr -d \'[:space:]\')"\n          if [[ -z "$expected_parent" ]]; then expected_parent="$current_main"; fi\n\n          if [[ "$expected_parent" != "$current_main" ]]; then'
if old not in text:
    raise SystemExit('RC lineage expected-parent marker missing')
text = text.replace(old, new, 1)
syntax = '''      - name: Check JavaScript syntax\n        shell: bash\n        run: |\n          set -euo pipefail'''
static = '''      - name: SOK commercialization static gate\n        shell: bash\n        run: |\n          set -euo pipefail\n          node deployment/qa/sok-commercialization-static.mjs\n          git diff --check\n\n'''
if 'SOK commercialization static gate' not in text:
    if syntax not in text:
        raise SystemExit('RC syntax step marker missing')
    text = text.replace(syntax, static + syntax, 1)
receipt = '      - name: Record release-candidate receipt'
smoke = '''      - name: Smoke SOK commercialization preview\n        shell: bash\n        env:\n          DEPLOYMENT_URL: ''' + expr('steps.deploy.outputs.deployment-url') + '''\n        run: |\n          set -euo pipefail\n          bash deployment/qa/sok-commercialization-preview.sh "$DEPLOYMENT_URL"\n\n'''
if 'Smoke SOK commercialization preview' not in text:
    if receipt not in text:
        raise SystemExit('RC receipt marker missing')
    text = text.replace(receipt, smoke + receipt, 1)
text = text.replace('RELEASE_LABEL: ' + expr('inputs.release_label'), 'RELEASE_LABEL: ' + expr("github.event_name == 'workflow_dispatch' && inputs.release_label || 'sok-commercialization-auto'"), 1)
text = text.replace('echo "Preview smoke: PASS" >> "$GITHUB_STEP_SUMMARY"', 'echo "Preview smoke: PASS" >> "$GITHUB_STEP_SUMMARY"\n          echo "SOK commercialization gate: PASS" >> "$GITHUB_STEP_SUMMARY"', 1)
write(p, text)

assert read('site/solar-project.html') == read('site/solar-project/index.html')
print('SOK commercialization build script: APPLIED')
