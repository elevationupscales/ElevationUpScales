(()=>{"use strict";const id=String(new URLSearchParams(location.search).get("id")||"").toLowerCase();const ids=new Set(["sok-sk12v100pc","sok-sk48v100n"]);if(!ids.has(id))return;const verified={"sok-sk12v100pc":{sku:"SK12V100PC",use:"RV, van, marine, backup and smaller off-grid systems",facts:["12.8V nominal","100Ah","1280Wh","Bluetooth","100A continuous discharge","Approx. 10.24 × 6.69 × 8.27 in"],warranty:"5-year manufacturer warranty"},"sok-sk48v100n":{sku:"SK48V100N",use:"48V solar, off-grid, backup and rack-storage systems",facts:["51.2V nominal","100Ah","5.12kWh","3U rack format","Integrated heater","RS485 / CANBus"],warranty:"7-year manufacturer warranty"}};const v=verified[id],track=(type,value,details={})=>window.EUSIntent?.track?.(type,value,{source:"sok-product",...details});fetch("/api/sok/catalog",{credentials:"same-origin",headers:{Accept:"application/json"}}).then(r=>r.ok?r.json():Promise.reject(new Error("catalog"))).then(d=>{const p=(d.products||[]).find(x=>String(x.id||"").toLowerCase()===id);if(!p)return;const summary=document.querySelector(".product-summary");if(!summary||document.querySelector("[data-sok-merch-panel]"))return;const trust=document.querySelector("[data-lithium-retailer]");if(trust){trust.hidden=false;trust.innerHTML="<strong>AUTHORIZED SOK ENERGY DEALER</strong>";}const buy=document.querySelector("[data-buy-now]");if(buy){buy.textContent=p.commerceCta||"See Purchase Options";buy.href=p.purchaseUrl||p.purchaseOptionsUrl;buy.dataset.sokPrimaryAction="true";}const freight=document.querySelector("[data-lithium-freight-link]");if(freight){freight.hidden=false;freight.href=`/sok-order.html?sku=${encodeURIComponent(v.sku)}&intent=hawaii&state=HI`;freight.textContent=String(p.hawaiiStatus||"").toLowerCase()==="shipping_available"?"Shipping to Hawaii? Review Hawaii options →":"Shipping to Hawaii? Freight Review Required →";}const note=document.querySelector(".product-note");if(note)note.textContent="Current purchase mode, destination and quantity determine whether the order uses checkout or assisted processing. Freight is confirmed separately where required.";const spec=(p.downloads||[]).find(x=>String(x.url||"").startsWith("/assets/brands/sok/"));const panel=document.createElement("section");panel.className="sok-merch-panel";panel.dataset.sokMerchPanel="true";panel.innerHTML=`<p class="eyebrow">SOK PRODUCT SUPPORT</p><h2>${v.sku} purchase options</h2><p>${v.use}. Final system compatibility is verified for the actual application.</p><div class="sok-merch-facts">${v.facts.map(x=>`<span>${x}</span>`).join("")}<span>${v.warranty}</span><span>${String(p.commerceLabel||"Current availability")}</span></div>${spec?`<a class="sok-merch-download" href="${spec.url}">Official ${v.sku} Specification Sheet</a>`:""}<div class="sok-merch-actions"><a class="button button-outline" data-sok-merch="commercial" href="/sok-order.html?sku=${encodeURIComponent(v.sku)}&intent=commercial">Request Commercial Pricing</a><a class="button button-outline" data-sok-merch="hawaii" href="/sok-order.html?sku=${encodeURIComponent(v.sku)}&intent=hawaii&state=HI">Check Hawaii Availability</a></div>`;summary.append(panel);panel.addEventListener("click",e=>{const a=e.target.closest("[data-sok-merch]");if(!a)return;track(a.dataset.sokMerch==="commercial"?"sok_commercial_request":"sok_hawaii_request",v.sku);});buy?.addEventListener("click",()=>track("sok_purchase_options_click",v.sku,{purchaseMode:p.publicPurchaseMode||""}));track("sok_product_view",v.sku,{availabilityMode:p.availabilityMode||"",purchaseMode:p.publicPurchaseMode||""});}).catch(()=>{});})();