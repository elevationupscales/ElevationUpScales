# Elevation UpScales — Peter Current Worktree

**Owner:** Casey Young  
**Human Manager:** Peter Torres  
**Parent:** Company Operations / current MPM  
**Status:** ACTIVE / SOK EBAY CURRENT UPLOAD WAVE  
**Effective:** 2026-09-24

## Current state

Peter's Git collaboration onboarding is complete.

Verified state:

- Peter GitHub identity: `cheekypete840`;
- current `main` is readable;
- authenticated repository write capability is verified;
- the Peter Handoff Log contains the successful access receipt;
- the stale access-verification gate is CLOSED.

Do not repeat onboarding unless a new access failure is actually verified.

## Current objective

Execute the bounded SOK eBay upload assignment already authorized on the canonical Current Work Board.

This Peter worktree does not create a new company priority. It activates Peter's local execution pointer to the already-current global assignment.

## Startup requirement

Before work:

**RESOLVE CURRENT MAIN → MASTER S.O.P. → CURRENT WORK BOARD → PETER MANAGER INDEX → EBAY STORE OPERATIONS WORKTREE → SOK PROJECT SOURCE → ASSIGNED UPLOAD WAVE.**

Required control sources:

- `../CURRENT_WORK_BOARD.md`
- `../EBAY_STORE_OPERATIONS_CURRENT_WORKTREE.md`
- `../vendor-project-sources/SOK_PROJECT_SOURCE.md`
- `PETER_MANAGER_INDEX.md`

## Active task

**SOK EBAY — EXECUTE CURRENT `00_CURRENT_UPLOADS` WAVE**

Execution rules:

1. work only from the supplied `00_CURRENT_UPLOADS` package/wave;
2. preserve SOK MAP controls and the assigned shipping-policy treatment;
3. verify the exact SKU/model and customer-facing listing data against the current SOK source before consequential listing action;
4. verify each upload/listing receipt before marking that item complete;
5. keep `10_PRESALE_HOLD` on hold until separately cleared;
6. do not use `90_SUPERSEDED_DRAFTS`;
7. do not reopen unrelated eBay recovery/catalog work merely because it exists historically;
8. record material execution results in `PETER_HANDOFF_LOG.md`.

## Completion condition

This assignment is complete when:

1. every item in the supplied `00_CURRENT_UPLOADS` wave has either a verified upload/listing receipt or an explicit blocker;
2. MAP and shipping controls remain intact;
3. no held presale item was activated;
4. no superseded draft was used;
5. any material state change is recorded in `PETER_HANDOFF_LOG.md` and routed upward when needed.

If an authenticated action surface is unavailable for a required eBay mutation, preserve the exact pending action and evidence as **OPEN TASK / ACTION SURFACE REQUIRED** rather than inventing completion.

## Boundaries

This worktree does not authorize:

- `10_PRESALE_HOLD` activation;
- use of `90_SUPERSEDED_DRAFTS`;
- unverified MAP/price changes;
- unverified shipping-policy changes;
- broad eBay catalog reprioritization;
- production website deployment;
- global Work Board reprioritization;
- Master S.O.P. changes;
- edits to another Project's Worktree;
- secrets or protected dealer data in public Git;
- external vendor email sends without Casey authorization.

## Return

**DONE:** current `00_CURRENT_UPLOADS` wave verified complete and receipts recorded.  
**BLOCKED:** preserve exact blocker/action required; do not broaden the block.  
**NEXT:** return verified result to Company Operations / current MPM and wait for the next bounded assignment.
