# ELEVATION OS 2.0 — START HERE

**For:** Peter Torres  
**Use:** One-time startup for Peter's new ChatGPT Project  
**Repository:** `elevationupscales/ElevationUpScales`

## Start here

This package is a **bootstrap**, not a second Operating System and not a permanent copy of company state.

Use it once to establish the new Peter ChatGPT Project.

After startup:

**RECOVER FROM GITHUB, NOT CHAT.**

The same Elevation GitHub repository becomes the ongoing source for current OS control, Work Board state, Peter's lane, vendor Projects, and current Worktrees.

## First setup

1. Create a new ChatGPT Project for Peter.
2. Add the package files to that Project.
3. Put `PETER_CHATGPT_PROJECT_START_PROMPT.md` into the Project instructions or use it as the first controlling prompt.
4. Connect Peter's own GitHub account to ChatGPT.
5. Confirm Peter has accepted the collaborator invitation for `elevationupscales/ElevationUpScales`.
6. Tell the new worker to begin startup.

Its first substantive action must be:

**RE-RESOLVE CURRENT `main` → READ CURRENT ACCEPTED OS CONTROL → READ CURRENT WORK BOARD → READ PETER INDEX/WORKTREE → RETURN GIT CHECK PASS.**

## Required first receipt

Do not accept a normal task recommendation before the worker returns:

**GIT CHECK PASS**  
**Current main:** `<resolved SHA>`  
**Controls read:** <current accepted control set>  
**Owning Project/Lane:** <resolved lane>  
**Current Worktree/Assignment:** <task or STANDBY>  
**Drift/Conflict:** <NONE or concise finding>

If it cannot access Git, the correct response is:

**GIT CHECK FAILED — CONNECT/RESTORE GITHUB ACCESS BEFORE GIT-DEPENDENT WORK.**

## Important

Do not treat the Work Board snapshot inside this ZIP as current authority after startup.

**PACKAGE TEACHES THE SYSTEM → LIVE GIT CONTROLS CURRENT STATE.**

Do not hard-code the package build SHA into the Project prompt. The worker resolves current `main` fresh.

## Peter lane rule

Peter's worker may read the public-safe global state it needs.

Normal write path:

**READ GLOBAL → WRITE LOCAL → ROUTE MATERIAL CHANGE UP.**

Peter-owned state lives under:

`operations/peter/`

Material company-level changes are returned through:

`operations/peter/PETER_HANDOFF_LOG.md`

using:

**MASTER UPDATE REQUIRED**

## After startup

This ZIP should not need routine replacement.

Future Peter chats should recover from:

**CURRENT GIT → CURRENT WORK BOARD → PETER INDEX/WORKTREE → OWNING PROJECT → NEXT ACTION.**
