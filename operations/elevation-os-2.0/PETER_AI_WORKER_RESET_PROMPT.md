# ELEVATION OS 2.0 — Peter AI Worker Reset

Use this only when a Peter-directed worker is showing stale-workflow behavior.

## Reset directive

Stop using old chat queues, old MPM numbers, historical handoffs, remembered vendor state, or previous local notes as current authority.

Do not restart the company from memory.

Immediately:

**RESOLVE CURRENT `main` → READ CURRENT ACCEPTED OS CONTROL → READ `CURRENT_WORK_BOARD.md` → READ PETER INDEX/WORKTREE → IDENTIFY OWNING PROJECT → READ CURRENT PROJECT SOURCE/WORKTREE → VERIFY ASSIGNMENT.**

Return:

**GIT CHECK PASS**  
**Current main:** `<SHA>`  
**Owning Project/Lane:** <lane>  
**Current Worktree/Assignment:** <task or STANDBY>  
**Stale pointers ignored:** <brief list or NONE>  
**Conflict requiring reconciliation:** <brief finding or NONE>

Then execute only the current authorized scope.

## Non-regression rules

- Do not create another Master Work Board.
- Do not replay completed, closed, cancelled, sold or superseded work.
- Do not hard-code historical MPM/COM/Recon chat numbers as current authority.
- Do not treat fresh evidence as self-granted management authority.
- Do not create duplicate vendor/listing/email/freight efforts when current work already exists.
- Do not inject internal control language into customer-facing work.
- Do not claim done/live/sent/published/deployed without verification.
- Do not write protected/private information into public Git.
- Do not substitute chat memory if Git-dependent current state cannot be resolved.

## Peter lane

Normal rule:

**READ GLOBAL → WRITE LOCAL → ROUTE MATERIAL CHANGE UP.**

If a verified Peter result materially changes global company state, write:

**MASTER UPDATE REQUIRED**

to:

`operations/peter/PETER_HANDOFF_LOG.md`

Do not silently rewrite company-wide priority.

## Control phrase

**IGNORE STALE CHAT STATE → RECOVER FROM GITHUB → CONTINUE EXISTING WORK → VERIFY BEFORE CLAIM.**
