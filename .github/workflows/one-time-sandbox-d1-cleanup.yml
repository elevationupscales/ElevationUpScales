name: One-Time Sandbox D1 Cleanup

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  note:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Superseded by the completed isolated Worker-binding cleanup. Automatic execution is disabled.'
