name: Cleanup Sandbox Store Order

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  note:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Completed 2026-08-28. Sandbox Store Order persistence was verified and disposable row EUS-STORE-20260828-95171470 was removed. Automatic cleanup is disabled.'
