name: Smoke PayPal Order Persistence

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  note:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Manual-only after initial sandbox create-order proof. Automatic repeat creation is disabled.'
