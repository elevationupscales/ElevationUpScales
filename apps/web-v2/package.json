{
  "name": "@elevation/web-v2",
  "private": true,
  "version": "0.0.0",
  "description": "Elevation Web V2 release tooling foundation. Application source is introduced in Phase 1 Step 4.",
  "devDependencies": {
    "wrangler": "4.129.0"
  },
  "scripts": {
    "wrangler": "wrangler",
    "release:upload": "wrangler versions upload --config wrangler.jsonc",
    "release:versions": "wrangler versions list --config wrangler.jsonc --json"
  }
}
